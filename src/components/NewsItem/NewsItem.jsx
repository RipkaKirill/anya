import React from 'react'
import s from './NewsItem.module.scss'

const NewsItem = ({ img, category, title, description }) => {
  return (
    <div className={s.newsItem}>
      item
    </div>
  )
}

export default NewsItem