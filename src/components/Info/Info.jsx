import React from 'react';
import PropTypes from 'prop-types';
//import { Test } from './Info.styles';

const Info = (props) => (
  <div className="InfoWrapper">
    Test content
  </div>
);

Info.propTypes = {
  // bla: PropTypes.string,
};

Info.defaultProps = {
  // bla: 'test',
};

export default Info;
